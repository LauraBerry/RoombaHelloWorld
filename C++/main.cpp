#include <stdio.h>
#include <iostream>
#include "bthcxn.cpp"

using namespace std;

int main(string args[])
{
	int num = atoi(args[1].c_str()); 		//not sure what this int is for I think it is the number of the bluetooth device
	string name=ars[2]; 					//not sure what this string is but I think it is the name of the bluetooth device
	cout << "Hello World!\n";
	return 0;
}
